# FocusFlow environment template
# Copy to `.env` or `.env.local` for local overrides.
# Only VITE_* keys are exposed to the browser bundle.

VITE_APP_NAME=FocusFlow
VITE_APP_ENV=development

# Public base path for Vite / Azure Static Web Apps (usually "/")
VITE_BASE_PATH=/

# When true, attempt Microsoft Teams host detection at runtime.
# Browser mode still works if detection fails.
VITE_TEAMS_ENABLED=true

# Microsoft Entra ID (leave empty for LOCAL mode)
VITE_AZURE_CLIENT_ID=
VITE_AZURE_TENANT_ID=
# Optional — defaults to https://login.microsoftonline.com/{tenantId}
VITE_AZURE_AUTHORITY=
VITE_AZURE_REDIRECT_URI=
VITE_AZURE_POST_LOGOUT_REDIRECT_URI=

# Legacy aliases (still supported)
# VITE_AZURE_AD_CLIENT_ID=
# VITE_AZURE_AD_TENANT_ID=
# VITE_AZURE_AD_REDIRECT_URI=

# Optional — set to true in Step 3B to activate live Entra login
# VITE_AZURE_AUTH_ENABLED=false

# Custom API scope for backend calls (defaults to api://{clientId}/access_as_user)
# VITE_AZURE_API_SCOPE=

# FocusFlow REST API (Sprint 4.4)
VITE_API_BASE_URL=http://localhost:3001/api

# Repository mode: api (default) | local
VITE_REPOSITORY_MODE=api
